# Use 'mariadb' as host because in GitHub Actions the service name acts as hostname for the container
# ${DB_HOST} will be replaced at runtime with environment variable DB_HOST or defaults to 127.0.0.1
db.url=jdbc:mariadb://${DB_HOST}:3307/hausverwaltung?useSSL=false&allowPublicKeyRetrieval=true
db.user=team
db.pw=team_pwd